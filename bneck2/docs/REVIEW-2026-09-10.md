# Review — where we are, progress, next steps (2026-09-10)

## 1. Where we are

One thesis, one engine, one loop. The thesis (goated.md): model the **moving
scarcity surface** — what increasing abundance makes newly scarce — with
Alpha = gap × dCF × cross-world-X × severity × irreducibility − crowdedness.
The engine (bneck2) implements it stdlib-only on $0 keyless data. The loop
(`scripts/oneclick.py`) runs all of it in one command and writes a
timestamped report.

State: **research prototype with live fire proven, not an investable
system.** 158/158 tests green. 348 verdicts, 126 signals, 84 severity
snapshots, 13 backtest scores, 18 experiment receipts on record.

## 2. Progress this session (all verified live)

- **Engine v2 built**: worlds/signal screen, LabSignal, digger ladder (L4
  gate), FTO map, CEO ledger, scarcity scanner, killfeed loop (SEC burst +
  OpenAlex attack + Polymarket/Kalshi/Manifold best-book + whale consensus),
  claims/belief board, kernel ports (backtest, ridge implied-p, Ma
  obsolescence), migration layer (B_i, dB/dt, X, P_release, catalytic edges,
  derivatives, alpha_v2, surprise, cliffs, duration mismatch, deliverable MW),
  consistency arbitrage, AI→Atoms screen, experimentation lab (cg-flow).
- **26 collectors live** across filings, insiders (SEC Form 4, whale
  wallets), 3 prediction venues + CLOB depth, research velocity (OpenAlex,
  bioRxiv, Crossref, HN, HF, OSTI), money trails (USAspending, Grants.gov,
  NSF), permissions (FedRegister, openFDA, trials, BLS), labs (OpenAI RSS,
  Anthropic hiring), fundamentals (XBRL companyfacts).
- **Importer packs unpacked properly** (flattened, hash-verified 35/35),
  10 trader-tracker repos cloned + experimented (polytrack runs here;
  recipes ported), venue API docs imported (`docs/VENUES.md`), canonical
  resource map (`docs/RESOURCES-CANONICAL.md`).
- **Two real catches**: CPO-testing bottleneck (4 independent sources) and
  NVDA's record $411M director sale + $1.09B overhang. Both correctly read
  as watch-not-buy (crowded / observation-grade).
- **Secrets scrubbed** from the tree (env-only keys, redacted archive).

## 3. Honest scorecard

| Area | Score | Note |
|---|---|---|
| Thesis formalism | 4/5 | Master eq + 18 extensions encoded; telemetry/actor/geo need feeds |
| Engine breadth | 4/5 | 20 collectors, 11 test files; ports honest about limits |
| Live proof | 3/5 | Verdicts + catches real; single-day history everywhere |
| Calibration | 2/5 | p_markets placeholder, absolute Form-4 thresholds, backtest INSUFFICIENT |
| Product surface | 2/5 | stockify unrunnable here (no pip); feeds not consuming engine |
| Ops (git/push) | 1/5 | Umbrella committed locally (7 commits); push blocked on valid token |

## 4. Next steps

**P0 (days, no new deps):**
1. Push umbrella (needs one valid token) — 7 commits ready at /home/ubuntu/killfeed.
2. Free `api.data.gov` signup → flips Congress/GovInfo/Regulations/EIA live.
3. Daily live passes (cron): verdicts + severity + panel mature on their own.
4. Resolve remaining unknowns: missing CIKs (8 tickers), Senate/House paths.

**P1 (weeks, needs deps/box):**
5. `uv sync` elsewhere → stockify suite green; feeds consume engine Signal.
6. pmxt adapter + venue-implied p_market (kills the placeholder).
7. Backtest-vetting for whale leaders (pmbot recipe) once pandas exists.
8. Kelly sizing + algo-fingerprints into ranking/forecasters.
9. edgartools filing ingestion (backlog/language diffs).

**P2 (months, needs history/capital-gating):**
10. 90-day brief history → backtest significance → paper portfolio vs baselines.
11. Usage-telemetry feed, actor/reaction graph, reverse-DCF mining, geo layer.
12. Postgres + backups + edge hardening before anything production-shaped.

## 5. Biggest risks, stated plainly

- Single-day history: every rate (dB/dt, baselines, panel) is an infant.
- Placeholder probabilities make consistency scores decorative until P1.6.
- Absolute thresholds will false-positive on routine filers until cadences mature.
- No capital, no trades, no executor anywhere — by design, keep it that way
  until P2.10 is green.
