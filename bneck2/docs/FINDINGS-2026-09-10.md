# Findings — sources, alternatives, venues (2026-09-10)

## Went live today

- **FINRA Reg SHO daily short volume** (CDN, per-symbol): NVDA 0.40, AMD
  0.56, MU 0.40, IONQ 0.59. 403 = unpublished-yet. (`finra.py`)
- **OpenInsider firehose** (plain HTTP only; 443 refused): ticker tape,
  cluster-buys, officer-buys. Tooltip-JS-safe parser. (`openinsider.py`)
- **Treasury FiscalData** (keyless): rates (Bills 3.788%), $40T debt clock.
  (`treasury.py`)
- **World Bank indicators** (keyless): GDP/R&D/hitech/energy per country.
  Note: Taiwan not a member (no data) — use KOR/CHN/USA. (`worldbank.py`)
- **House Clerk YTD index** (keyless ZIP): 1609 filings, 381 PTRs with
  member/doc/date. Per-filing PDFs need parsing (queued). (`house.py`)

## Alternatives found (web + clones)

- **Congress trades**: seralifatih/congress-trading-pipeline (Senate EFD
  JSON + House ZIP→PDF + Apify hosted), DMulajkar/Quantgress (18-dataset
  Quiver-rebuild: 13F, short vol, patents, donors, pageviews, exec comp —
  recipe mine), crnicholson/capitol-api (free self-hosted House API),
  CongressInvests (claims free tier, endpoints 404 — queued),
  Disclosed Capitol (free tier w/ key), politicianstocktracker (waitlist).
  Senate direct: session-only (Quantgress confirms). Our path: House index
  now → PDF parse later → Senate session last.
- **Gov MCPs**: lzinga/us-gov-open-data-mcp cloned (42 APIs/343 tools, TS
  SDK sans MCP; keyless half already covered natively). GovInfo ships its
  own public MCP (preview). FRED MCPs (stefanoamorelli, shanehull) need
  FRED keys — skip until api.data.gov signup.
- **SEC MCPs**: cmanohar/mcp-edgar, kevinkda/sec-edgar-mcp (read-only,
  10 tools), birthday-tools/edgarmcp (adds FRED/NPORT/OpenFIGI),
  btopn/OpenInsider-MCP (16 tools — recipes ported, not the runner).
  Pattern: all wrap the same public endpoints we hit natively; stdlib
  ports beat Node runners on this box (no npm haute for TS).
- **unignorant** (prx0r): World Bank Data360 (keyless OData) earmarked for
  geo layer; HDX/IATI need registered ids.

## Still blocked (with path)

Senate (session), NIH (405), SemScholar pool (429s), Lever slugs,
3 lab feeds, USPTO/EPO (registration), FTD/CUSIP, House PDFs (needs pdf
lib), FRED/BEA/EIA/Census (one api.data.gov key), Metaculus (auth),
Kalshi per-trader positions (structurally impossible — track books).
