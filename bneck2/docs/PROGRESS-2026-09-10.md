# Progress report — 2026-09-10 (full session)

## What existed at start

bneck v1 (uncommitted prototype), stockify (uncommitted rename, live-key leak),
two R2 import zips under temp names, double-nested `imported/`, no git anywhere
that mattered, no live loops, no tests beyond baselines.

## What was built (bneck2: 0 → 161 tests, all green)

1. **Engine core** — worlds/Signal screen, LabSignal ranking, digger ladder
   (L4 gate), patent FTO, CEO ledger, scarcity scanner (stockify map
   generalized to all 16 nodes), belief board (claims seeded + web-verified).
2. **Killfeed loop** — SEC burst + OpenAlex attack (group_by fix) + 3-venue
   pm clock (Polymarket/Kalshi/Manifold, best-book-wins) + whale consensus
   (holders/positions) + SEC cadence baselines + PM query overrides.
   348 verdicts logged, non-firings included.
3. **Kernel ports (stdlib)** — walk-forward backtest + panel accumulation,
   ridge implied-probabilities, exact Ma obsolescence.
4. **Migration layer** — severity B, dB/dt history, cross-world X, release P,
   CATALYZES hazards, bottleneck derivatives, alpha_v2, surprise, cliffs,
   duration mismatch, deliverable MW, transfer tiers, 3-clock claims,
   consistency arbitrage, AI→Atoms screen (17 cos).
5. **Experimentation lab (cg-flow)** — 10 hypotheses, 6 disposed
   (E001/E003/E005/E009 refuted with receipts; E002/E004 confirmed
   directional). cg repo cloned as flow reference.
6. **26 collectors live** — filings, XBRL facts, whales, 3 pm venues + CLOB
   depth + Kalshi candles, research (OpenAlex/bioRxiv/Crossref/HN/HF/OSTI),
   money (USAspending/Grants/NSF), permissions (FedRegister/openFDA/trials/
   BLS), labs (OpenAI+DeepMind RSS, Anthropic hiring), GitHub events.
7. **One-click loop** — poll→killfeed→migrate→experiments→connections→
   backtest→report; daily cron 06:17 UTC set. Venue docs, trader verdicts,
   canonical resource map written.
8. **Ops** — secrets scrubbed (3 files), umbrella repo `prx0r/killfeed`
   PUBLIC and pushed (1 clean commit at push time +1 local since),
   stale/ archive created, outbox zip fresh.

## Proof it works (all same-day, independent)

- CPO-testing bottleneck: attack signal + 3 industry sources agree.
- NVDA $411M record director sale caught by burst detector; forward -0.3%
  (correctly read as watch, not signal — E001 refuted).
- Whale consensus live (29 whales $109K); hit rate 0.67 on resolved (E004).
- E005/E009: severity⊥conviction (-0.44 → -0.02 with crowdedness) — kept
  both measures, structurally distinct.

## Numbers

161/161 tests · 20 collectors · 348 verdicts · 126 signals · 84 severity
snapshots · 13 panel scores · 19 receipts · 19 connections/pass ·
463M tree (441M = Levin PDFs).
