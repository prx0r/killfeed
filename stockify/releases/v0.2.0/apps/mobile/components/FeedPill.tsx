import type { Feed } from "@feedify/core";import { Pressable,StyleSheet,Text,View } from "react-native";import { useTheme } from "@/lib/theme";import { router } from "expo-router";
export function FeedPill({feed}: {feed:Feed}){const t=useTheme();return <Pressable onPress={()=>router.push(`/feed/${feed.slug}` as never)} style={[s.wrap,{backgroundColor:t.card,borderColor:t.line}]}><View style={[s.icon,{backgroundColor:t.accent}]}><Text style={s.iconText}>{feed.emoji}</Text></View><View style={{flex:1}}><Text numberOfLines={1} style={[s.name,{color:t.text}]}>{feed.name}</Text><Text style={[s.sub,{color:t.muted}]}>{format(feed.subscriberCount)} subs</Text></View></Pressable>}
const format=(n:number)=>n>999?`${(n/1000).toFixed(n>9999?0:1)}k`:String(n);const s=StyleSheet.create({wrap:{width:174,borderRadius:20,borderWidth:1,padding:14,marginRight:10},icon:{width:40,height:40,borderRadius:13,alignItems:"center",justifyContent:"center",marginBottom:20},iconText:{fontSize:19,fontWeight:"800",color:"#151612"},name:{fontSize:16,fontWeight:"700"},sub:{fontSize:12,marginTop:4}});
